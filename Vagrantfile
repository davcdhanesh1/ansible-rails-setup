# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.box = "my-vagrant-box"
  config.vm.box_url = "./boxes/trusty64.box" 
  config.vm.network "private_network", ip: "192.168.33.10"
end
